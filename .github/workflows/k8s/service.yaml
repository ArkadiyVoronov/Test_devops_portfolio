apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio
spec:
  type: ClusterIP  # Или LoadBalancer для облака
  selector:
    app: devops-portfolio
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP